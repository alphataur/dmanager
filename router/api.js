const { Store, Messages, SendJSON, HTTXDownloader, TorrentDownloader } = require("../utils")
const express = require("express")
const router = express.Router()


router.all("/httx/add", (req, res) => {
  if(req.method === "POST"){

  }
  else if(req.method === "GET"){

  }
  else{

  }
})
